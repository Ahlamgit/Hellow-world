{
  "name": "khadamati",
  "version": "0.1.0",
  "private": true,
  "description": "KHADAMATI V1 monorepo — Sprint 0 foundation",
  "workspaces": [
    "apps/admin-web",
    "apps/store-web",
    "packages/shared"
  ],
  "scripts": {
    "build:web": "npm run build --workspace admin-web && npm run build --workspace store-web",
    "dev:admin": "npm run dev --workspace admin-web",
    "dev:store": "npm run dev --workspace store-web"
  }
}
