-- User Management Module
-- No schema changes required; uses existing Users, UserProfiles, Roles, UserRoles tables.
-- Deploy after 012_IdentityModule.sql

PRINT 'User Management module uses existing identity tables. No DDL required.';
