spring:
  datasource:
    url: jdbc:h2:mem:khadamati;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true

khadamati:
  dev-mode: true
  jwt:
    secret: test-secret-key-minimum-32-characters-long
